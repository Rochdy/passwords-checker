{
    "rules": [
        {
            "regex": ".{5,}",
            "message": "The minimum length of the password should be 5 characters"
        },
        {
            "regex": "[0-9]",
            "message": "The password must contains at least one digit"
        },
        {
            "regex": "^(?!.*(\\w)\\1{2,}).+$",
            "message": "The password must not have no more than two repeating consecutive characters"
        },
        {
            "regex": "[A-Z]|[\\W]",
            "message":  "The password must contains at least one upper-case character OR one special character"
        }
    ]
}